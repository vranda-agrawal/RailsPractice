<%= form_with(model: booktitle) do |form| %>
  <% if booktitle.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(booktitle.errors.count, "error") %> prohibited this booktitle from being saved:</h2>

      <ul>
        <% booktitle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
